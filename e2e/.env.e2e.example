# E2E Test Environment Configuration
# Copy this to .env.e2e and fill in your values

# Base URL for E2E tests (HTTPS with self-signed cert expected)
E2E_BASE_URL=https://admin.core-platform.local

# Path to Monitoring UI (adjust based on your routing)
E2E_MONITORING_PATH=/monitoring

# Realm 1 (admin) credentials
E2E_USERNAME=test_admin
E2E_PASSWORD=admin123

# Realm 2 credentials (for tenant isolation tests - optional)
# E2E_USERNAME_REALM2=test_user_regular
# E2E_PASSWORD_REALM2=user123

# Keycloak SSO URL (if using programmatic login)
# E2E_KC_URL=https://sso.core-platform.local/realms/admin/protocol/openid-connect/token
# E2E_KC_CLIENT_ID=core-frontend-public

# Timeouts (milliseconds)
E2E_TIMEOUT_DEFAULT=60000
E2E_TIMEOUT_EXPECT=15000

# Debug options
# E2E_HEADLESS=false  # Set to false to see browser during tests
# E2E_SLOWMO=100      # Slow down test execution (ms per action)
