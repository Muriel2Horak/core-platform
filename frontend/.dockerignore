# Ignore build output - Docker will rebuild it
# NOTE: dist/ is NOT ignored - esbuild creates it during build and we need to copy it
# dist/
node_modules/
.cache/

# Ignore development files
*.log
.DS_Store
.env.local
