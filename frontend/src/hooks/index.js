/**
 * Hooks Barrel Export
 * Central export point for all custom React hooks
 */

export { default as usePermissions } from './usePermissions';
export {
  useEntityView,
  useEntityMutation,
  useEntityList,
  useOptimisticUpdate,
} from './useEntityView';
