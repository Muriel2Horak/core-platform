# S4: Navigation Patterns & Menu System

**EPIC:** [EPIC-004: UX/UI & Design System](../README.md)  
**Status:** 🔵 **TODO**  
**Priority:** P1  
**LOC:** ~600 řádků  
**Effort:** ~8 hodin

---

## 📋 Story Description

Jako **user**, chci **intuitivní navigaci s breadcrumbs, side menu a tab navigation**, abych **se snadno orientoval v aplikaci**.

---

## 🎯 Acceptance Criteria

### AC1: Sidebar Navigation
- **GIVEN** admin panel
- **WHEN** otevřu aplikaci
- **THEN** vidím:
  - Collapsible sidebar
  - Menu sections (Dashboard, Users, Settings)
  - Active page highlight
  - Icon + label
  - Collapsed mode (only icons)

### AC2: Breadcrumbs
- **GIVEN** nested page (Admin > Users > Edit)
- **WHEN** zobrazím breadcrumbs
- **THEN** vidím:
  - Clickable path (Admin > Users > Edit)
  - Current page highlighted
  - Separator icons

### AC3: Tab Navigation
- **GIVEN** detail stránka s tabs
- **WHEN** přepínám tabs
- **THEN**:
  - Active tab highlighted
  - URL updates (query param)
  - Tab content lazy loads

---

## 📦 Implementation

### Sidebar Menu
```tsx
export const AppSidebar: React.FC = () => {
  const [collapsed, setCollapsed] = useState(false);
  
  return (
    <Drawer variant="permanent" sx={{ width: collapsed ? 60 : 240 }}>
      <List>
        <ListItemButton onClick={() => setCollapsed(!collapsed)}>
          <ListItemIcon><MenuIcon /></ListItemIcon>
        </ListItemButton>
        
        <NavSection title="Dashboard" icon={<DashboardIcon />}>
          <NavItem to="/admin/overview" label="Overview" />
          <NavItem to="/admin/analytics" label="Analytics" />
        </NavSection>
        
        <NavSection title="Users" icon={<PeopleIcon />}>
          <NavItem to="/admin/users" label="All Users" />
          <NavItem to="/admin/groups" label="Groups" />
        </NavSection>
      </List>
    </Drawer>
  );
};
```

---

**Related:** [EPIC-004 README](../README.md#s4-navigation-patterns)
