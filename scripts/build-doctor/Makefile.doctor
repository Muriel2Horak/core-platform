.PHONY: doctor-pre-build
doctor-pre-build:
	@if [ "$${SKIP_DOCTOR:-false}" = "true" ]; then \
		echo "⚠️  SKIP_DOCTOR=true - skipping Build Doctor pre-flight checks"; \
		exit 0; \
	fi
	@bash scripts/build-doctor/pre-build-checks.sh
